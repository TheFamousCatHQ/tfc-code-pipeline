[tool.poetry]
name = "tfc-test-writer-aider"
version = "0.1.0"
description = "A tool to assist with writing tests for TFC"
authors = ["Michael Lahr <michael.lahr@gmail.com>"]
readme = "README.md"
packages = [
    {include = "tfc_test_writer_aider", from = "src"},
    {include = "find_source_files", from = "src"},
    {include = "explain_code", from = "src"},
    {include = "write_tests", from = "src"},
    {include = "write_token_utils_tests", from = "src"}
]

[tool.poetry.dependencies]
python = ">=3.11"
python-dotenv = "^1.0.0"

[tool.poetry.scripts]
tfc-test-writer = "tfc_test_writer_aider.cli:cli"
find-source-files = "find_source_files:main"
explain-code = "explain_code:main"
write-tests = "write_tests:main"
write-token-utils-tests = "write_token_utils_tests:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
